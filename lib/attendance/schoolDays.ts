/** Default semester length used until an admin sets a school-specific total via the
 * academic_terms table (see app/api/admin/academic-terms/route.ts). */
export const DEFAULT_TOTAL_INSTRUCTIONAL_DAYS = 90;
